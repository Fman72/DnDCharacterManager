import react from 'react';
import { AbilityUse } from '../../../types/api';

interface AbilityUseLogProps {
    abilityUses: AbilityUse[];
}

const AbilityUseLog = (props: AbilityUseLogProps) => {
    const { abilityUses } = props;
    
}